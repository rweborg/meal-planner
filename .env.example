# Copy to .env and fill in. Used for local dev and as reference for Railway.

# Postgres connection (required – use Railway Postgres URL or local/Neon/Supabase)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Claude API key (required for recipe generation)
ANTHROPIC_API_KEY="your-anthropic-api-key"

# Optional: Unsplash API key for recipe images (get from unsplash.com/developers)
# UNSPLASH_ACCESS_KEY="your-unsplash-access-key"
